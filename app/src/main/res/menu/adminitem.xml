<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="Check Users"
        app:showAsAction="always"
        android:id="@+id/CheckUsers"
        android:icon="@drawable/ic_profile"
        android:checkable="true"
        android:checked="true"/>
    <item android:title="Check Available seats"
        app:showAsAction="always"
        android:id="@+id/AvailableSeats"
        android:icon="@drawable/cinema"
        android:checkable="true"/>
    <item android:title="Check Booked Tickets"
        app:showAsAction="always"
        android:id="@+id/bookedTickets"
        android:icon="@drawable/booked"
        android:checkable="true" />
</menu>